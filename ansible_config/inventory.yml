---
all:
  children:
    database:
      hosts:
        db-vm:
          ansible_host: 0.0.0.0
          ansible_user: root
    webserver:
      hosts:
        webserver-vm:
          ansible_host: 0.0.0.0
          ansible_user: root
    client:
      hosts:
        client-vm:
          ansible_host: 0.0.0.0
          ansible_user: root

